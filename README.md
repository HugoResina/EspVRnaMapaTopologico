# EspVRnaMapaTopologico

f -> create point
s -> save image
p -> draw visual point
m -> move selected point
button in the middle of the point -> displays id and selects said point when clicked
enter -> save chosen parameters(top input -> meters[250-1000], botom input -> num sensors[1-10])
